<?php

/*

TODO LIST:

- You've matched
- Pro Member flag
- View full profile link
- Cool stats
- Link to feedback
- Check cache??
- Log in tm_match if you follow back

Bugs:

- Mobile freeze

Suggestions algorithm:

- Follows you
- Members in your chapter
- Members in your country
- Members in your segment
- Pro Members
- Featured Members (via conf)
- Chapter Leaders
- I'm lucky members
- Logged in in past 24 hours
- Lots of followers
- Filter out private, reported profiles
- Don't show yourself
- Not people you are already following


Extas:

- Add calendar
- Look into whereby.com

DONE!

- Demo mode
- Hook into notification
- Demo for non-signed in, non-approved
- Pass in UID
- Debug mode


*/

// tm_match.module
include_once 'tm_match.display.inc';
include_once 'tm_match.controller.inc';
include_once 'tm_match.callbacks.inc';

/**
 * Implements hook_menu().
 */
function tm_match_menu() {

	$items = array();

	$items['match'] = array(
		'title' => 'Match',
		'page callback' => 'tm_match_display_index',
		'access arguments' => array('access content')
	);

	$items['match/ajax/cards'] = array(
		'title' => 'Match',
		'page callback' => 'tm_match_fetch_cards_ajax',
		'access arguments' => array('access content')
	);

	$items['match/callback/left/%/%'] = array(
		'title' => 'Swipe Left',
		'page callback' => 'tm_match_callback_swipe_left',
		'page arguments' => array(3, 4),
		'access arguments' => array('access content'),
	);

	$items['match/callback/right/%/%'] = array(
		'title' => 'Swipe Right',
		'page callback' => 'tm_match_callback_swipe_right',
		'page arguments' => array(3, 4),
		'access arguments' => array('access content')
	);

	return $items;
}



