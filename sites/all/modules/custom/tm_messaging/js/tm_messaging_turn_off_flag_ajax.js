(function ($, Drupal, window, document, undefined) {jQuery(document).ready(function() {

// turn flag handlers off
// this will make the link refresh the page so user can see additional actions
$(".flag").off();

});})(jQuery, Drupal, this, this.document);